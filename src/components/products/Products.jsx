

const Products = (a, b) => {
   const sum = a+b;
   return a+b
};

const evenNumber = (a) => {
    if (a % 2 === 0) {
        
        return 'this number is even'
    }
    return "this number is odd"
}

export {Products, evenNumber}
